const SponsoredBanner = () => (
  <div className="inline-flex items-center gap-1.5 px-3 py-1 rounded-full bg-warm-clay/10 text-warm-clay text-xs font-medium border border-warm-clay/20 mb-3">
    <span>Sponsored</span>
  </div>
);

export default SponsoredBanner;
